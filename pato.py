lista = [a, b, c, d, e, f, g, h]
def patopato(lista, posicion):
    while posicion > len(lista):
      posicion = posicion - len(lista)
    posicion = posicion + 1
    print(lista[posicion])
    return 
input

